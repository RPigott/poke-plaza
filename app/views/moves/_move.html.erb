<% if move %>
    <%= content_tag(:div,
        move.name,
        :class => ["pokemon-move", "pokemon-move-#{move.type.name.downcase}", ("pokemon-move-egg" if egg)])
    %>
<% else %>
    <%= tag(:div,
        :class => ["pokemon-move", "pokemon-move-none"])
    %>
<% end %>
